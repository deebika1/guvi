# TO CHECK IT IS PRIME OR NOT
# all=[] is used to store the all elements in the list
# coprime[] is used to store the non prime numbers
# prime[] is used to store the prime numbers

all=[]
coprime=[]
prime=[]
ct=0
a=int(input("enter the no of elements"))
for i in range(0,a):
 b=int(input("enter the elements"))
 all.append(b)
print(all)
for i in range(1,a):
    if(all%1==0 and all%all==0):
     ct=ct+1


if (ct==2):
 prime.append(all)
else:
 prime.append(all)
 
 
